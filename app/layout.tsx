import type { Metadata } from "next";
import { Space_Grotesk, Inter } from "next/font/google";
import "./globals.css";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  display: "swap",
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Portfolio | Software Developer",
  description: "A world-class portfolio showcasing exceptional frontend skills with sophisticated 3D animations and modern design",
  keywords: ["portfolio", "software developer", "frontend", "3D animations", "web development"],
  authors: [{ name: "Your Name" }],
  openGraph: {
    title: "Portfolio | Software Developer",
    description: "A world-class portfolio showcasing exceptional frontend skills",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${spaceGrotesk.variable} ${inter.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
